name: Supabase Keep-alive
on:
  schedule:
    - cron: "0 0 * * *"
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send HEAD request to Supabase
        run: |
          curl -I "${{ secrets.SUPABASE_URL }}/rest/v1/" \
          -H "apikey: ${{ secrets.SUPABASE_ANON_KEY }}"
